DATABASE_URL="postgresql://iosbid:iosbid@localhost:5433/iosbid?schema=public"

# Stripe — get these from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY="sk_test_replace_me"
# From `npm run stripe:listen` (starts with whsec_)
STRIPE_WEBHOOK_SECRET="whsec_replace_me"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Set to "1" to skip Stripe entirely and settle bids instantly (local dev only).
BYPASS_PAYMENTS="0"
